set logscale x 2
#set logscale y
set autoscale
set xlabel "number of elements"
set ylabel "time"
set title "Time vs size"
#set xtic 20
set terminal png size 1000,700
plot "poly.col" using 1:2 with lines title "data", [32000:200000] 1.3*32000/x + 3.4 * (x-32000) / x
#pause 100
#latency(BufferSize) = (Sum as i < current_cache_level of
#                            CacheSize[i] * CacheLatency[i] / (BufferSize not in lower caches))
#                       + CacheLatency[current_cache_level] * (BufferSize not in lower caches) / BufferSize
